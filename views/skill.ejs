<div class="container">
  <div class="row">
    <div class='col-md-5 col-md-offset-1'>
      <img class="th" src="http://placehold.it/400x400">
    </div>
    <div class='col-md-5'>
      <h1>{{skill.skillTitle}}</h1>
      <h3>{{skill.skillCategory}}</h3>
      <h3>{{skill.name}}</h3>
      <p>{{skill.description}}</p>
      <button class="btn btn-success">Trade with {{}}</button>
    </div>
  </div>
</div>